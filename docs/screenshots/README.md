# Current-theme screenshots

Captured from the actual installed v1.2.0 theme in MyUSF Canvas on September 15, 2026, using a 1512 × 805 desktop viewport.

| Image | Capture scope |
| --- | --- |
| `terminal-overview.jpg` | Top workbench and surrounding decorative margins, ending before the native dashboard content. The navigation/avatar area is excluded. |
| `theme-controls.jpg` | Interior of the opaque theme settings panel, excluding its surroundings and rounded corners. |

Only cropped screenshots were saved. No full-page image containing coursework was stored in the repository. Both files were visually inspected before commit; neither contains student names, avatars, course titles/codes, assignments, grades, personal URLs, or browser tabs. The visible `unova@canvas` terminal prompt is static theme text, not an account identifier.

No blur, generative edits, invented sample coursework, or design changes were applied. These are static frames of animated effects. The controls show the appearance preferences at capture time, not necessarily the extension's defaults.

The live page was returned to the top with its settings panel closed. Course content, completion checkboxes, and academic records were not changed.
